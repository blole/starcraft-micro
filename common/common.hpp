#pragma once

#include <memory>
using std::shared_ptr;
using std::unique_ptr;

#include <boost/range.hpp>
using boost::sub_range;

#include <vector>
#include <deque>
#include <list>
using std::vector;
using std::deque;
using std::list;
